# Copy to .env (gitignored) and fill in from the LinkedIn developer portal:
# https://www.linkedin.com/developers/apps -> your app -> Auth -> Application credentials
# Only needed locally, to run linkedin_auth.py. The bot itself reads GitHub secrets.

BCM_LI_CLIENT_ID=
BCM_LI_CLIENT_SECRET=

ROA_LI_CLIENT_ID=
ROA_LI_CLIENT_SECRET=
